# URL of the Express backend server.
# Leave EMPTY when using Vite's built-in /api proxy (vite.config.ts → proxy → localhost:8080).
# Set to http://localhost:8080 only if running the frontend without the Vite dev server proxy.
VITE_SERVER_URL=
